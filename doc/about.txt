AIS: what it is, and what it is not
===================================

Your memory, yours to keep.
Models average everyone. Keep what's only yours.

WHAT IT IS

  An extension of your associative memory. A memo that stays with you and stays
  yours, bringing things back the way your mind does -- by association. You file
  anything (a link, a file, a note, a document) under one or more keys, and
  recall it by those keys. Underneath, it is an index.

  A working memex. In 1945 Vannevar Bush imagined the "memex," a personal device
  that extends memory by association; he never built it. AIS is one, on the
  durable, private side of that idea -- plain text you own, that no service can
  read. The idea is Bush's and widely prior; AIS's contribution is the
  implementation (see PROVENANCE below).

  An index, not a vault. It does not absorb your files. Short notes can live
  inside (`--doc`), but your documents stay where you keep them; AIS stores only
  a reference (a path or a URL) and never copies, moves, or reads inside them.
  Your storage stays immutable and organized your way, and you lay the index you
  want over it - even several indexes over the same untouched files. The index
  is a view; your data is never touched. That separation is the core idea.

  Non-linear. One item can live under many keys; items that share a key recall
  together. Keys cross-link, so the index is a graph of associations, not a
  flat list of folders or a single timeline.

  Yours, free, repairable - by guarantee, not promise. The index is append-only
  plain text on your own device (store, idx/, blobs/). There is no server, so
  nothing phones home and no one, not even the author, can read your keys or
  your memory. GPL keeps it free, open, and forkable: no vendor can enclose it
  or shut it down. Plain text keeps it repairable: a bad byte costs a line, not
  the store, and `cat` still reads it in fifty years, long after the apps and
  formats holding your other data are gone. That is the hard part of an
  archive: programs change, plain text does not. Copy it to an SD card,
  delete it, or move it to other storage with cp, then read it right there with
  any text viewer, on a machine that has never heard of AIS. Your backup is a
  folder you can read by hand, not a sealed blob only the app can reopen. You
  verify all of this (read the license, read the files, watch the network); you
  don't trust it.

  One engine, thin front-ends. The CLI is the contract; over a small FFI seam
  sit the web GUI (`ais --serve`, any browser), the Flutter mobile app, and a
  native Win32 desktop wrapper (niche/legacy). A browser PWA/WASM build is a
  planned future track, not shipped. Not tied to one browser or one vendor.

  Bounded and quick. It streams: memory stays flat whether the index holds ten
  records or ten million, and an offset index keeps recall fast.

  Universal. Anything you can name with keys: bookmarks, files, shell commands,
  phone numbers, and multi-line documents (`ais --doc`).

  Encryptable, and safe to hand to an agent. A value or a whole document can be
  stored encrypted inline (`-e`). Decryption is interactive: the cleartext
  appears only after you supply the passphrase, at a terminal or in the app, and
  never when the output is piped, dumped, synced, or read programmatically. So an
  AI agent reading your index sees an opaque `aisc:` marker, not the secret, while
  still using everything around it as context. There is no unlocked vault to
  drain, and no master key: each secret is its own blob, opened one at a time, by
  you.

WHAT IT IS NOT

  Not just another bookmark saver. It can save bookmarks, but it is a general,
  key-addressed store, not a browser feature.

  Not a browser extension. It does not live inside Chrome or Firefox, and does
  not sync through a vendor's account.

  Not a cloud service. No sign-up, no account to lose, no ads, no tracking.

  Not full-text search. It matches the keys YOU assign, not the body of what
  you store (`find` searches the values and paths, not a document's contents).

  Not a database server. No SQL, no daemon, no schema, just text files and a
  small C program.

  Not a file vault or document manager. It indexes your files where they live;
  it does not take them in, convert them, or lock them inside an app. Your
  storage stays untouched and immutable.

  Not a password manager. No autofill, no generation, no breach alerts, no
  shared vaults, so for hundreds of web logins a dedicated manager is more
  convenient. But for a secret that lives with its context, AIS is the safer
  thing to put in front of an agent: wiring a vault to an agent exposes the whole
  vault, while an AIS secret stays sealed unless you decrypt it yourself, at a
  terminal or in the app.

WHERE YOUR DATA LIVES

  Plain files you can find, copy, back up, edit, or delete yourself, under
  ~/.ais by default. See USING.txt for the per-OS paths, `ais --where`,
  `ais --default`, and `ais --init`.


PROVENANCE

  AIS's authorship and priority record (2001 -> 2026) is a citable deposit:

      doi.org/10.5281/zenodo.20647048
